# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
# Local secrets and Firebase web config (use .env.example as a template)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase Auth export dumps (contain password hashes)
test-users.json
users-temp.json

# Local Admin SDK key for scripts/provision-user.mjs
scripts/serviceAccountKey.json
**/serviceAccountKey.json
!scripts/serviceAccountKey.json.example
# MCP config may contain Bearer tokens (use mcp.json.example as template)
.cursor/mcp.json

# Convex auto-generated types (regenerated by npx convex dev)

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
